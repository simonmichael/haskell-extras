#!/usr/bin/env runghc

-- Usage: script-runghc (Windows: runghc script-runghc)
-- or:    runghc -package aeson -package csv -package tabular -package text script-runghc
-- A haskell script using GHC's runghc script (https://downloads.haskell.org/~ghc/latest/docs/html/users_guide/runghc.html).
-- runghc and ghc must be in $PATH.
-- To import from packages, install them in the global package db (eg with cabal install PKG --lib),
-- then specify them on the command line as shown above.
-- A .hs file extension is optional.
-- Tested with ghc 8.10.4.

-- import Data.Aeson
-- import qualified Data.Text as T
-- import Text.CSV
-- import Text.Tabular

main = do
  putStrLn "hello"
